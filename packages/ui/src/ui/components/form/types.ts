/**
 * Defines the properties for the SubmitButton component, including states for disabled and submitting status.
 *
 * @param disabled Whether the button should be visually and interactively disabled.
 *
 * @param isSubmitting Whether the form is currently in a submitting state.
 */
export type SubmitButtonProps = {
  disabled: boolean
  isSubmitting: boolean
}
