export enum Direction {
  Up,
  Down,
  Left,
  Right,
}

export enum Color {
  Red = 1,
  Green,
  Blue,
}

export enum Status {
  Pending = 'pending',
  Running = 'running',
  Completed = 'completed',
}
